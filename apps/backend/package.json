{
  "name": "@agent-ts/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Dev server unavailable in offline stub'",
    "build": "bunx --bun tsc -p tsconfig.build.json",
    "start": "echo 'Start command not supported in offline stub'",
    "lint": "echo 'Linting disabled in offline stub'"
  }
}
